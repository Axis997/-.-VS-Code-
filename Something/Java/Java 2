Условие: найти второй по величине элемент массива.
Ответ:
import java.util.*;

public class Main {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        System.out.print("Введите количество элементов: ");
        int n = sc.nextInt();

        int[] arr = new int[n];
        System.out.println("Введите элементы массива:");
        for (int i = 0; i < n; i++) {
            arr[i] = sc.nextInt();
        }

        Arrays.sort(arr);
        int max = arr[n - 1];
        int second = -1;

        for (int i = n - 2; i >= 0; i--) {
            if (arr[i] < max) {
                second = arr[i];
                break;
            }
        }

        if (second == -1)
            System.out.println("Второго максимума нет (все элементы равны)");
        else
            System.out.println("Второй по величине элемент: " + second);
    }
}
