#include <iostream>
using namespace std;

int main() {
    int n;
    cout << "Введите количество элементов: ";
    cin >> n;

    int arr[n];
    int even = 0, odd = 0;

    cout << "Введите элементы массива:\n";
    for (int i = 0; i < n; i++) {
        cin >> arr[i];
        if (arr[i] % 2 == 0)
            even++;
        else
            odd++;
    }

    cout << "Чётных чисел: " << even << endl;
    cout << "Нечётных чисел: " << odd << endl;
    return 0;
}
